const AMAZING_GAME_TEMPLATE = [
  "${NAME} popped off for a ${PTS} points over ${MIN} minutes, adding ${3PTM} triples, ${REB} rebounds, ${AST} assists and ${STL} steals.",
];

module.exports = { AMAZING_GAME_TEMPLATE };
